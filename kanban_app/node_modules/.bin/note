#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../note/bin/note" "$@"
  ret=$?
else 
  node  "$basedir/../note/bin/note" "$@"
  ret=$?
fi
exit $ret
